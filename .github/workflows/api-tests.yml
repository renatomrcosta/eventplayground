#name: API-tests

#on: [push, pull_request]

#jobs:
#  gradle:
#    strategy:
#      matrix:
#        os: [ubuntu-latest, macos-latest]
#    runs-on: ${{ matrix.os }}
#    steps:
#      - uses: actions/checkout@v2
#      - uses: actions/setup-java@v1
#        with:
#          java-version: 15
#      - name: Run simple gradle tests
#        run: make test-api
